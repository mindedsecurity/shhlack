{
  "name": "shhlack",
  "version": "1.0.5",
  "description": "Shhlack is an encryptor/decryptor or peer to peer messages in Slack.It relies on pre shared keys between the peers so only the peers with the right passphrase will be able to see the encrypted message.",
  "main": "build.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@mindedlab:stefano/shhlack.git"
  },
  "author": "Minded Security",
  "license": "ISC",
  "dependencies": {
  }
}
